# UIE Entity Extraction Training — StructBERT + BIO token classification
# Backbone: uer/structbert-base-chinese (same as ModelScope UIE)
torch>=2.0
transformers>=4.30
accelerate>=0.20
datasets>=2.14
evaluate>=0.4
