# Data

This directory keeps small local samples that make the NER pipeline understandable.

Ignored local artifacts:

- `data/ner/processed/`: generated by `python -m src.ner.preprocess`
- large database dumps
- temporary experiment outputs

The public repository should not include production data, private database exports, or large generated datasets.
